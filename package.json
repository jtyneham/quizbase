{
  "name": "quizbase-v2",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/app/app.mjs && node --test tests/*.test.mjs"
  }
}
