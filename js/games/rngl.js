import { initRandomLetter } from "../core/random-letter-engine.js";

// Game wrapper intentionally stays thin. A themed build can provide a
// different visual renderer without changing routing or the shared engine.
export { initRandomLetter };
